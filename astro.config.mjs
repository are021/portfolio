// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
//
// Deployed as a GitHub Pages PROJECT site: https://are021.github.io/portfolio
// If you rename the repo to "are021.github.io" (a user site), change to:
//   site: 'https://are021.github.io',
//   base: '/',
export default defineConfig({
  site: 'https://are021.github.io',
  base: '/portfolio/',
});
